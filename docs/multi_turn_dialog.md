# 多轮对话

## 目的
打开**memory**之门
在上一个模块，大家已经发现，这个llm call是无法跨请求获得信息的（有些厂商的API，可以选择开启你只需要返回一个ID，然后在下一次请求中，将这个ID作为参数传入，可以在远端存储消息对话），我们只有实现多轮对话，你的AI才能开始有memory

## 流程
1. 多轮对话实现原理（实现add_message方法）
2. 实现能够对话的CLI（log = silence）
3. 永久memory实现
